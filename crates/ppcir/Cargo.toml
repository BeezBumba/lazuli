[package]
name = "ppcir"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-only"
description = "PowerPC stack-machine IR for the Lazuli GameCube emulator (inspired by Play!'s Jitter)"

[dependencies]
gekko = { workspace = true }

[lints]
workspace = true
